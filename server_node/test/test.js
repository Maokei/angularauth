const assert = require('assert')

describe("bonk", () => {
    context("Function to be tested", () => {
        it('should do something', () => {
            assert.equal(1,1);
        });
    });
});
